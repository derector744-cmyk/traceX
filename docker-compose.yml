version: "3.9"

services:
  tracex:
    build: .
    image: tracex:latest
    container_name: tracex
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - tracex_data:/app/data

volumes:
  tracex_data:
